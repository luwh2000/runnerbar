# AI 实操题 B3：AI 换脸 Demo（含合规与风控）（产品培训生方向）

**岗位**：产品培训生（全栈工程师｜实现产品）
**预计耗时**：60-90 分钟（提交窗口 3 天）

---

## 一、业务背景

换脸（face swap）技术在影像创作、娱乐和影视制作中有一定应用价值，但也存在巨大的滥用风险（肖像权侵犯、诈骗、伪造内容等）。

对于“实现产品”的工程角色来说，我们希望看到你不仅能做出一个可运行 Demo，还能把**合规边界与风控设计**落实到产品与工程实现中。

## 二、任务要求

请你用 AI 工具辅助，搭建一个“受控的换脸 Demo”。

### 必做部分

1. **受控输入（合规前置）**
   - Demo 必须只处理你有权使用的素材。
   - 你需要在 `README.md` 中明确：你使用的输入素材来源，以及你拥有使用权的说明。

2. **核心能力实现（必须可运行）**
   - 输入：
     - `source_face.jpg`（被替换的人脸）
     - `target_image.jpg` 或 `target_video.mp4`（目标图/视频）
   - 输出：
     - `output.jpg` 或 `output.mp4`
   - 要求：
     - 尽量保持目标画面的光照/肤色一致性（不要求电影级，但要可用）
     - 处理失败时要有清晰的错误提示（例如：检测不到人脸、角度过大等）

3. **风控与防滥用设计（必须实现至少 2 条）**
   - 你需要在 Demo 中实现可验证的限制措施，例如：
     - 仅允许处理本地白名单素材（固定目录/固定文件名/手工确认）
     - 强制添加可见水印（例如“Demo only / For evaluation”）
     - 输出图自动写入元信息标记（如 EXIF/侧边记录）
     - 检测到公众人物/多人人脸时拒绝处理（你可以用简化规则实现）

### 加分部分（如有余力）

4. 做一个简单 UI（如 Streamlit）：上传素材 → 显示风控提示 → 生成结果。

## 三、提交物

请将以下内容打包为一个**项目文件夹**（命名建议：`姓名-B3`），通过回复发题邮件提交（压缩包/网盘链接均可）。

### 必交清单（缺一视为提交不完整）

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | **可运行 Demo** | 脚本/Notebook/网页均可 |
| 2 | **README.md** | 如何安装与运行、模型/工具来源、风控策略说明、取舍与限制、项目文件结构 |
| 3 | **requirements.txt** | 或等效依赖清单，确保评审人可一键安装 |
| 4 | **预生成的输出文件** | `output.jpg` 或 `output.mp4`，确保评审人不运行代码也能看到结果 |
| 5 | **演示录屏** | 不超过 3 分钟的 MP4，录制 Demo 运行全过程（启动→输入→风控提示→输出） |
| 6 | **AI 使用记录** | 如何使用 AI 完成任务：关键步骤、提示词示例、迭代过程（可作为独立文件或 README 的一节） |
| 7 | **自校验清单** | 不同角度/不同光照的稳定性、失败案例处理、风控策略是否可绕过（可作为独立文件或 README 的一节） |

## 四、提交方式与时间

- 提交截止：【由发题时确定】
- 如需延期，请在截止前主动联系并说明原因
